# Copia este archivo a ".env.local.ps1" (mismo folder) y completa tus valores reales.
# ".env.local.ps1" esta en .gitignore - nunca se sube al repo.
$env:FARMAMIA_DB_PASSWORD = "tu-password-real-de-postgres"
$env:FARMAMIA_JWT_SECRET = "cualquier-cadena-larga-y-aleatoria-para-firmar-jwt"
